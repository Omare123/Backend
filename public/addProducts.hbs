<div class="d-flex flex-column align-items-center w-25 h-25" >
    <img src="{{image}}" alt="{{name}}" width="100" height="100">
    <span>{{name}}</span>
    <span>${{price}}</span>
    <button class="btn btn-primary" id="{{_id}}">Agregar</button>
</div>